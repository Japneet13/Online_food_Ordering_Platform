<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>User Orders | Admin Panel</title>
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <h1>📦 Orders for User ID: <%= userId %></h1>
  <a href="/admin/users" class="btn back-btn">← Back to Users</a>

  <% if (orders.length === 0) { %>
    <p>This user hasn’t placed any orders yet.</p>
  <% } else { %>
    <table class="admin-table">
      <thead>
        <tr>
          <th>Order ID</th>
          <th>Total Items</th>
          <th>Total Price</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% orders.forEach(order => { %>
          <tr>
            <td><%= order.id %></td>
            <td><%= order.total_items %></td>
            <td>₹<%= order.total_price %></td>
            <td><%= new Date(order.created_at).toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
